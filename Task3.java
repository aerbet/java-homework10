public class Task3 {
    
    public static void main(String[] args) {
        double x = 1;
        double equation1 = -1.0 / Math.pow(x, 2); // a

        double a = 5;
        double b = 2;
        double c = 2;
        double equation2 = a / (b * c); // б

        double equation3 = (a / b) * c; // в
        
        double equation4 = (a + b) / 2; // г

        double equation5 = 5.45 * (a + 2 * b) / (2 - a); // д
        
        double equation6 = (-b + Math.sqrt(Math.pow(b, 2) - 4 * a * c)) / (2 * a); // е
        
        double equation7 = (-b + 1.0 / a) / (2.0 / c); // ж
        
        double equation8 = 1.0 / (1 + (a + b) / 2.0); // з
        
        double equation9 = 1.0 / (1 + 1.0 / (2 + 1.0 / (2 + 3.0 / 5.0))); // и
        
        double m = 1;
        double n = 1;
        double equation10 = Math.pow(2, Math.pow(m, n)); // к
    }
}
